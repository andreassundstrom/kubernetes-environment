apiVersion: v1
kind: Service
metadata:
    name: keycloak-postgres-db
spec:
    selector:
        app: {{ .Values.database.appName }}
    type: LoadBalancer
    ports:
    - port: 5432
      targetPort: 5432